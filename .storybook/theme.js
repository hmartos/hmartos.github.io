import { create } from '@storybook/theming/create';
import 'theme/styles.scss';

export default create({
    base: 'light',
    brandTitle: 'Landing Page Components',
    brandUrl: 'https://hmartos.xyz',
    brandImage: '',
});
